<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ManChingu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="Theme.ManChingu" parent="Base.Theme.ManChingu" />

    <style name="CustomRatingBar" parent="Theme.MaterialComponents.Light">
        <item name="colorControlActivated">@color/light_blue</item> <!-- Warna bintang terisi -->
        <item name="colorControlNormal">@color/white</item>     <!-- Warna bintang kosong -->
    </style>

</resources>